// Physical Geography — Rivers. Empty until verified data is added; see districts.js for
// the schema shape every entry here should follow (districts[], division, coordinates,
// optional path[] for a hand-curated course line exactly like Bharat's rivers use).
export const RIVER_LOCATIONS = [];
